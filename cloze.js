

//Clozecard constructor

function ClozeCard(front, back) {
	this.front = front;
	this.back = back;

	// Look at argument 3 (the statement) to see if argument 4 is contained in it.
	if (indexof.Clozecard >= this[4]){
		//If arugment 4 is part of argument 3, replace it with "..." .
		remove(this[4] from index) replace with('...')
	}
}

							
var ClozeCard = new ClozeCard('front', 'back')

console.log(Flashcard)

module.exports = ClozeCard;


